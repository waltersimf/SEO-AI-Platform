# 🔧 v0.3.1 MINI-SPRINT - Production Ready

**Дата:** 23.11.2025  
**Тривалість:** 1 робочий день (5-6 годин)  
**Мета:** Критичні фікси перед v0.4 AI Teammate

---

## 🎯 ЗАГАЛЬНА МЕТА

**1 робочий день критичних фіксів що блокують production deployment і погіршують UX.**

Це не нові features - це **виправлення технічного боргу**.

**Після v0.3.1 → v0.4 AI Teammate! 🤖**

---

## 📋 ЗМІСТ

- [Таск #1: Прибрати localhost hardcode (2 год)](#таск-1-прибрати-localhost-hardcode-2-години)
- [Таск #2: Socket.io connection status (2 год)](#таск-2-socketio-connection-status-2-години)
- [Таск #3: Auto-logout на 401 (1 год)](#таск-3-auto-logout-на-401-1-година)
- [Timeline](#timeline)
- [Acceptance Criteria](#acceptance-criteria)
- [Як робитимемо](#як-робитимемо)

---

## 🔥 ТАСК #1: Прибрати localhost hardcode (2 години)

### ❌ Проблема зараз:

```typescript
// apps/web/src/components/chat/chat-list.tsx
const response = await fetch('http://localhost:4000/api/chat/list', {
  headers: { Authorization: `Bearer ${token}` }
});

// apps/web/src/app/dashboard/layout.tsx
const socket = io('http://localhost:4000');

// І так у 15+ місцях! 😱
```

**Чому це погано:**
- ❌ НЕ можна деплоїти на Vercel/Railway
- ❌ НЕ можна показати інвесторам (тільки локально)
- ❌ Кожен deploy = ручна заміна 15+ рядків
- ❌ Блокує production testing

---

### ✅ Рішення:

#### Крок 1: Створити `.env` файли

```bash
# apps/web/.env.local (development)
NEXT_PUBLIC_API_URL=http://localhost:4000

# apps/web/.env.production (production)
NEXT_PUBLIC_API_URL=https://api.forgeline.com
```

**Важливо:** `.env.local` додати в `.gitignore`!

---

#### Крок 2: Створити config file

```typescript
// apps/web/src/config/api.ts
export const API_URL = process.env.NEXT_PUBLIC_API_URL || 'http://localhost:4000';
export const WS_URL = API_URL.replace('http', 'ws');

// Для Socket.io
export const SOCKET_URL = API_URL;
```

---

#### Крок 3: Замінити всюди hardcode

**Було:**
```typescript
const response = await fetch('http://localhost:4000/api/chat/list', {
  headers: { Authorization: `Bearer ${token}` }
});
```

**Стало:**
```typescript
import { API_URL } from '@/config/api';

const response = await fetch(`${API_URL}/api/chat/list`, {
  headers: { Authorization: `Bearer ${token}` }
});
```

**Було:**
```typescript
const socket = io('http://localhost:4000');
```

**Стало:**
```typescript
import { SOCKET_URL } from '@/config/api';

const socket = io(SOCKET_URL);
```

---

#### Файли для редагування:

**Frontend (apps/web/src/):**
1. `components/chat/chat-list.tsx`
2. `components/chat/chat-box.tsx`
3. `components/chat/create-chat-dialog.tsx`
4. `app/dashboard/layout.tsx`
5. `components/google/google-connect-button.tsx`
6. `app/auth/login/page.tsx`
7. `app/auth/signup/page.tsx`
8. `app/google-callback/page.tsx`
9. Будь-які інші файли з `fetch('http://localhost:4000`

**Backend (якщо є hardcode):**
- Перевірити `apps/api/src/` на hardcode URLs

---

#### Команда для Claude Code:

```
Replace all localhost hardcode with environment variables:

1. Create config file:
   - File: apps/web/src/config/api.ts
   - Export: API_URL, SOCKET_URL from process.env.NEXT_PUBLIC_API_URL

2. Create .env files:
   - apps/web/.env.local with NEXT_PUBLIC_API_URL=http://localhost:4000
   - apps/web/.env.production with NEXT_PUBLIC_API_URL=https://api.forgeline.com

3. Replace in ALL files:
   - Replace 'http://localhost:4000' with ${API_URL}
   - Import API_URL from '@/config/api'
   - For Socket.io: use SOCKET_URL

4. Files to edit (~15 files):
   - apps/web/src/components/chat/*
   - apps/web/src/app/dashboard/layout.tsx
   - apps/web/src/app/auth/*
   - Any file with fetch('http://localhost:4000')

Test after changes: pnpm dev should work on localhost
```

---

#### Результат:

- ✅ `pnpm dev` → працює на localhost
- ✅ Deploy на Vercel → працює на production
- ✅ Одна зміна `.env` → змінюється всюди
- ✅ Готові до production deployment

---

## 🟢 ТАСК #2: Socket.io connection status (2 години)

### ❌ Проблема зараз:

**User scenario:**
1. Користувач відкриває чат
2. WiFi відключається на 30 секунд
3. Користувач пише повідомлення → натискає Send
4. **НІЧОГО НЕ ВІДБУВАЄТЬСЯ** 🤷‍♂️
5. Користувач не розуміє чому - нема індикатора!

**Dashboard показує "Connection Status" (пункт #3) але не працює!** 😤

---

### ✅ Рішення:

#### UI Mockup:

```
┌────────────────────────────────┐
│ Dashboard                      │
│                                │
│ What's Next? (v0.3 completion) │
│                                │
│ ✓ Online Status                │
│   See who's online real-time   │
│                                │
│ 3 Connection Status            │
│   🟢 Connected                 │ ← Зелений коли OK
│   ⚪ Reconnecting...           │ ← Сірий коли lost
│   🔴 Disconnected              │ ← Червоний якщо error
└────────────────────────────────┘
```

---

#### Крок 1: Додати state в layout.tsx

```typescript
// apps/web/src/app/dashboard/layout.tsx

const [socketStatus, setSocketStatus] = useState<'connected' | 'disconnected' | 'reconnecting'>('disconnected');

useEffect(() => {
  const socket = io(SOCKET_URL);
  
  // Connected
  socket.on('connect', () => {
    console.log('✅ Socket connected');
    setSocketStatus('connected');
  });
  
  // Disconnected
  socket.on('disconnect', () => {
    console.log('❌ Socket disconnected');
    setSocketStatus('disconnected');
  });
  
  // Reconnecting
  socket.io.on('reconnect_attempt', () => {
    console.log('🔄 Socket reconnecting...');
    setSocketStatus('reconnecting');
  });
  
  // Connection error
  socket.on('connect_error', (error) => {
    console.error('🔴 Connection error:', error);
    setSocketStatus('disconnected');
  });
  
  return () => {
    socket.disconnect();
  };
}, []);
```

---

#### Крок 2: Створити компонент ConnectionStatus

```typescript
// apps/web/src/components/connection-status.tsx

interface ConnectionStatusProps {
  status: 'connected' | 'disconnected' | 'reconnecting';
}

export function ConnectionStatus({ status }: ConnectionStatusProps) {
  const config = {
    connected: {
      icon: '🟢',
      label: 'Connected',
      color: 'text-green-600',
      bgColor: 'bg-green-500/20'
    },
    reconnecting: {
      icon: '⚪',
      label: 'Reconnecting...',
      color: 'text-gray-600',
      bgColor: 'bg-gray-500/20'
    },
    disconnected: {
      icon: '🔴',
      label: 'Disconnected',
      color: 'text-red-600',
      bgColor: 'bg-red-500/20'
    }
  };
  
  const current = config[status];
  
  return (
    <div className="flex items-center gap-2">
      <span className="text-lg">{current.icon}</span>
      <span className={`text-sm font-medium ${current.color}`}>
        {current.label}
      </span>
    </div>
  );
}
```

---

#### Крок 3: Показати на Dashboard

```typescript
// apps/web/src/app/dashboard/page.tsx

import { ConnectionStatus } from '@/components/connection-status';

export default function DashboardPage() {
  // ... existing code
  
  return (
    <div>
      {/* ... existing content ... */}
      
      <div className="border-t border-primary/20 pt-6 mt-6">
        <h3 className="font-semibold mb-3">What's Next? (v0.3 completion)</h3>
        <div className="grid gap-3 sm:grid-cols-2">
          
          {/* Existing: Online Status */}
          <div className="flex items-start gap-3 text-sm">
            <div className="flex h-6 w-6 items-center justify-center rounded bg-green-500/20 text-green-600">
              ✓
            </div>
            <div>
              <p className="font-medium">Online Status</p>
              <p className="text-muted-foreground">
                See who's online real-time 🟢
              </p>
            </div>
          </div>
          
          {/* NEW: Connection Status */}
          <div className="flex items-start gap-3 text-sm">
            <div className="flex h-6 w-6 items-center justify-center rounded bg-primary/20 text-primary">
              3
            </div>
            <div>
              <p className="font-medium">Connection Status</p>
              <ConnectionStatus status={socketStatus} />
            </div>
          </div>
          
        </div>
      </div>
    </div>
  );
}
```

---

#### Крок 4 (Bonus): Toast при disconnect/reconnect

```typescript
import { toast } from 'sonner';

socket.on('disconnect', () => {
  setSocketStatus('disconnected');
  toast.error('Connection lost. Reconnecting...', {
    id: 'socket-status' // Prevent duplicate toasts
  });
});

socket.on('connect', () => {
  setSocketStatus('connected');
  toast.success('Connected!', {
    id: 'socket-status'
  });
});
```

---

#### Команда для Claude Code:

```
Add Socket.io connection status indicator to Dashboard:

1. Update dashboard/layout.tsx:
   - Add socketStatus state (connected/disconnected/reconnecting)
   - Add socket event listeners: connect, disconnect, reconnect_attempt
   - Pass socketStatus to page component via props

2. Create components/connection-status.tsx:
   - Component that shows 🟢 Connected / ⚪ Reconnecting / 🔴 Disconnected
   - Different colors for each status
   - Use Tailwind classes for styling

3. Update dashboard/page.tsx:
   - Import ConnectionStatus component
   - Show in "What's Next?" section as item #3
   - Replace placeholder text with actual status

4. (Optional) Add toast notifications:
   - On disconnect: "Connection lost. Reconnecting..."
   - On connect: "Connected!"

Test: Disconnect WiFi and check if status changes!
```

---

#### Тестування:

1. Запустити `pnpm dev`
2. Відкрити Dashboard
3. Подивитись статус (має бути 🟢 Connected)
4. **Відключити WiFi на 10 секунд**
5. Статус має змінитись на ⚪ Reconnecting...
6. Увімкнути WiFi
7. Статус має повернутись на 🟢 Connected

---

#### Результат:

- ✅ Користувач завжди бачить статус connection
- ✅ Зрозуміло чому повідомлення не відправляється
- ✅ Dashboard "Connection Status" (пункт #3) працює!
- ✅ Professional UX

---

## 🔐 ТАСК #3: Auto-logout на 401 (1 година)

### ❌ Проблема зараз:

**User scenario:**
1. Користувач логінився вчора (JWT дійсний 7 днів)
2. Сьогодні відкрив Dashboard → все ОК (token є в localStorage)
3. Через 10 хвилин токен протухає на backend
4. Користувач клікає на чат → **ERROR 401** в консолі
5. Сторінка порожня/зламана, користувач не розуміє що сталося
6. Треба вручну йти на `/auth/login`

**Це погано для UX і security!** 😤

---

### ✅ Рішення:

#### Створити API wrapper з централізованим error handling

---

#### Крок 1: Створити `lib/api.ts`

```typescript
// apps/web/src/lib/api.ts

import { API_URL } from '@/config/api';
import { toast } from 'sonner';

/**
 * Wrapper для fetch з централізованим error handling
 * Автоматично додає Authorization header
 * Автоматично обробляє 401 (expired token)
 */
export async function apiFetch(
  endpoint: string,
  options: RequestInit = {}
): Promise<Response> {
  const token = localStorage.getItem('token');

  const response = await fetch(`${API_URL}${endpoint}`, {
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...(token && { Authorization: `Bearer ${token}` }),
      ...options.headers,
    },
  });

  // ⚠️ КРИТИЧНО: Обробка 401 Unauthorized
  if (response.status === 401) {
    console.log('🔐 Token expired - logging out');
    
    // Clear token
    localStorage.removeItem('token');
    
    // Show toast
    toast.error('Session expired. Please login again.', {
      duration: 5000,
    });
    
    // Hard redirect (no router needed)
    window.location.href = '/auth/login';
    
    throw new Error('Unauthorized');
  }

  // Обробка інших помилок
  if (!response.ok) {
    const error = await response.json().catch(() => ({}));
    throw new Error(error.message || 'Request failed');
  }

  return response;
}

/**
 * Helper для JSON responses
 */
export async function apiFetchJson<T>(
  endpoint: string,
  options?: RequestInit
): Promise<T> {
  const response = await apiFetch(endpoint, options);
  return response.json();
}
```

---

#### Крок 2: Замінити всі fetch() на apiFetch()

**Приклад 1: Chat List**

```typescript
// apps/web/src/components/chat/chat-list.tsx

// Було:
const token = localStorage.getItem('token');
const response = await fetch('http://localhost:4000/api/chat/list', {
  headers: {
    Authorization: `Bearer ${token}`,
  },
});
const chats = await response.json();

// Стало:
import { apiFetchJson } from '@/lib/api';

const chats = await apiFetchJson<Chat[]>('/api/chat/list');
```

**Приклад 2: Send Message**

```typescript
// apps/web/src/components/chat/chat-box.tsx

// Було:
const token = localStorage.getItem('token');
await fetch(`http://localhost:4000/api/chat/${chatId}/read`, {
  method: 'POST',
  headers: {
    Authorization: `Bearer ${token}`,
  },
});

// Стало:
import { apiFetch } from '@/lib/api';

await apiFetch(`/api/chat/${chatId}/read`, {
  method: 'POST',
});
```

**Приклад 3: Google OAuth**

```typescript
// apps/web/src/components/google/google-connect-button.tsx

// Було:
const response = await fetch(`http://localhost:4000/api/google/status`, {
  headers: {
    Authorization: `Bearer ${localStorage.getItem('token')}`,
  },
});

// Стало:
import { apiFetchJson } from '@/lib/api';

const status = await apiFetchJson<GoogleStatus>('/api/google/status');
```

---

#### Файли для редагування (пріоритетні):

**Critical (must fix):**
1. `components/chat/chat-list.tsx`
2. `components/chat/chat-box.tsx`
3. `app/dashboard/layout.tsx`
4. `components/google/google-connect-button.tsx`
5. `components/chat/create-chat-dialog.tsx`

**Medium priority:**
6. `app/google-callback/page.tsx`
7. Будь-які інші файли з `fetch()` що використовують token

**Low priority (auth pages - токена ще немає):**
- `app/auth/login/page.tsx`
- `app/auth/signup/page.tsx`

---

#### Крок 3 (Optional): Створити React hook

```typescript
// apps/web/src/hooks/useApi.ts

import { apiFetch, apiFetchJson } from '@/lib/api';

/**
 * React hook для API calls
 * Можна додати loading state, error handling тощо
 */
export function useApi() {
  const call = async <T = any>(
    endpoint: string,
    options?: RequestInit
  ): Promise<T> => {
    try {
      return await apiFetchJson<T>(endpoint, options);
    } catch (error) {
      console.error('API call failed:', error);
      throw error;
    }
  };

  return { call };
}

// Usage:
// const { call } = useApi();
// const chats = await call<Chat[]>('/api/chat/list');
```

---

#### Команда для Claude Code:

```
Add centralized API error handling with auto-logout on 401:

1. Create lib/api.ts:
   - Function apiFetch(endpoint, options)
   - Auto-add Authorization header from localStorage
   - On 401: clear token + toast + redirect to /auth/login
   - Export apiFetchJson helper

2. Replace ALL fetch() calls with apiFetch():
   Priority files:
   - apps/web/src/components/chat/chat-list.tsx
   - apps/web/src/components/chat/chat-box.tsx
   - apps/web/src/app/dashboard/layout.tsx
   - apps/web/src/components/google/google-connect-button.tsx
   - apps/web/src/components/chat/create-chat-dialog.tsx

3. Pattern:
   OLD: fetch(url, { headers: { Authorization: Bearer ${token} } })
   NEW: apiFetch(endpoint) // token auto-added

4. Benefits:
   - Token expired → auto-logout with message
   - No manual token management
   - Centralized error handling

Test: Delete token from localStorage, try to fetch data - should auto-logout
```

---

#### Тестування:

**Test Case 1: Normal flow**
1. Login → Dashboard
2. Все працює ✅

**Test Case 2: Expired token**
1. Login → Dashboard
2. В DevTools → Application → localStorage → видалити token
3. Спробувати відкрити чат або будь-який API call
4. Має з'явитись toast "Session expired"
5. Має redirect на /auth/login ✅

**Test Case 3: Backend token expiry**
1. Login → Dashboard
2. На backend змінити JWT expiry на 10 секунд (для тесту)
3. Почекати 15 секунд
4. Спробувати API call
5. Має бути auto-logout ✅

---

#### Результат:

- ✅ Токен протух → auto-logout + повідомлення
- ✅ Користувач розуміє що сталося
- ✅ Не треба F5 або вручну логінитись
- ✅ Централізована обробка помилок (1 місце замість 15)
- ✅ Security: expired tokens не використовуються

---

## ⏱️ TIMELINE

```
День 1 (23.11.2025):

09:00 - 11:00: Таск #1 - Remove localhost hardcode
├─ 09:00-09:15: Створити .env файли
├─ 09:15-09:30: Створити config/api.ts
├─ 09:30-10:30: Замінити в 15+ файлах (Claude Code)
└─ 10:30-11:00: Тестування localhost + production URL

11:00 - 13:00: Таск #2 - Connection status indicator
├─ 11:00-11:30: Додати socket events в layout.tsx
├─ 11:30-12:00: Створити ConnectionStatus компонент
├─ 12:00-12:30: Інтегрувати в Dashboard page
└─ 12:30-13:00: Тестування (disconnect WiFi!)

13:00 - 14:00: 🍔 Обід

14:00 - 15:00: Таск #3 - Auto-logout на 401
├─ 14:00-14:20: Створити lib/api.ts wrapper
├─ 14:20-14:50: Замінити fetch в 5-10 пріоритетних файлах
└─ 14:50-15:00: Тестування (expired token)

15:00 - 15:30: Final testing + CHANGELOG
├─ 15:00-15:15: Перевірити всі 3 фікси разом
├─ 15:15-15:25: (Optional) Deploy на staging
└─ 15:25-15:30: Оновити CHANGELOG.md

15:30: ✅ v0.3.1 COMPLETE!

TOTAL: 5.5 годин (з обідом)
      4.5 години чистого часу
```

---

## ✅ ACCEPTANCE CRITERIA

**v0.3.1 вважається завершеним якщо:**

### 1. Environment Variables (Таск #1)
- ✅ `.env.local` створено з `NEXT_PUBLIC_API_URL=http://localhost:4000`
- ✅ `.env.production` створено з production URL
- ✅ `config/api.ts` експортує `API_URL` і `SOCKET_URL`
- ✅ Всі hardcoded `http://localhost:4000` замінені на `${API_URL}`
- ✅ `pnpm dev` працює на localhost
- ✅ (Bonus) Build працює з production URL

### 2. Connection Status (Таск #2)
- ✅ Dashboard показує connection status (🟢/⚪/🔴)
- ✅ Статус змінюється при disconnect WiFi
- ✅ Статус повертається при reconnect
- ✅ "Connection Status" (пункт #3) працює на Dashboard
- ✅ (Bonus) Toast notification при disconnect/connect

### 3. Auto-logout (Таск #3)
- ✅ `lib/api.ts` створено з `apiFetch()` function
- ✅ При 401 відбувається: clear token → toast → redirect
- ✅ Мінімум 5 файлів використовують `apiFetch()` замість `fetch()`
- ✅ Тест: видалити token → спробувати API call → auto-logout працює
- ✅ (Bonus) Всі fetch() замінені на apiFetch()

### 4. Documentation
- ✅ CHANGELOG.md оновлено з v0.3.1 changes
- ✅ Commit message: "v0.3.1: Production ready fixes"

---

## 🔧 ЯК РОБИТИМЕМО

### Hybrid Workflow (як у v0.3):

**Claude Chat:**
1. Ти кажеш "почнімо v0.3.1"
2. Я даю детальний план для Claude Code
3. Ти копіюєш команду в Claude Code

**Claude Code:**
4. Claude Code робить зміни
5. Показує PR / diff

**Перевірка:**
6. Ти дивишся зміни
7. Merge PR
8. `git pull` локально
9. Тестуєш: `pnpm dev`

**Feedback:**
10. Працює? ✅ → Наступний таск
11. Не працює? ❌ → feedback в Claude Chat

**Repeat для 3 тасків!**

---

## 🎯 НАВІЩО ЦЕ ВСЕ?

### Без v0.3.1:
- ❌ НЕ можна показати інвесторам (тільки localhost)
- ❌ НЕ можна деплоїти на production
- ❌ Погана UX (connection lost, expired tokens)
- ❌ Технічний борг накопичується
- ❌ Hardcode в 15+ файлах

### З v0.3.1:
- ✅ **Production-ready** (можна деплоїти)
- ✅ **Professional UX** (користувач завжди розуміє що відбувається)
- ✅ **Чистий код** (без костилів і hardcode)
- ✅ **Security** (auto-logout на expired tokens)
- ✅ **Готові до v0.4 AI Teammate!** 🤖

---

## 📊 OPTIONAL: Якщо є час (+3-5 годин)

### Bonus Task #1: DTO Validation (3 години)

**Backend validation з class-validator:**

```typescript
// apps/api/src/auth/dto/signup.dto.ts

import { IsEmail, IsString, Length, IsNotEmpty } from 'class-validator';

export class SignupDto {
  @IsEmail({}, { message: 'Invalid email format' })
  email: string;

  @IsString()
  @Length(6, 50, { message: 'Password must be 6-50 characters' })
  password: string;

  @IsString()
  @IsNotEmpty()
  @Length(2, 100)
  name: string;

  @IsString()
  @IsNotEmpty()
  organizationName: string;
}
```

**Enable globally:**
```typescript
// apps/api/src/main.ts

app.useGlobalPipes(new ValidationPipe({
  whitelist: true, // Strip unknown properties
  forbidNonWhitelisted: true, // Throw error on unknown properties
  transform: true, // Auto-transform payloads
}));
```

---

### Bonus Task #2: useApi Hook (2 години)

**Create React hook wrapper:**

```typescript
// apps/web/src/hooks/useApi.ts

import { useState } from 'react';
import { apiFetchJson } from '@/lib/api';

export function useApi<T>() {
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null);

  const call = async (endpoint: string, options?: RequestInit): Promise<T | null> => {
    setLoading(true);
    setError(null);
    
    try {
      const data = await apiFetchJson<T>(endpoint, options);
      return data;
    } catch (err) {
      const message = err instanceof Error ? err.message : 'Unknown error';
      setError(message);
      return null;
    } finally {
      setLoading(false);
    }
  };

  return { call, loading, error };
}
```

**Usage:**
```typescript
const { call, loading, error } = useApi<Chat[]>();

const chats = await call('/api/chat/list');
if (loading) return <Spinner />;
if (error) return <Error message={error} />;
```

---

## 🎉 ПІСЛЯ ЗАВЕРШЕННЯ v0.3.1

**Готово до:**
1. ✅ Deploy на staging/production
2. ✅ Демо для інвесторів
3. ✅ v0.4 AI Teammate development
4. ✅ Scaling (production traffic)

**Next milestone:**
- **v0.4 - AI Teammate** 🤖
- Estimated: 2-3 тижні
- Killer feature для інвесторів!

---

**Last Updated:** 22.11.2025 (Evening)  
**Status:** 📋 PLANNED  
**Next:** Start v0.3.1 on 23.11.2025

---

🚀 **Let's make it production-ready!**